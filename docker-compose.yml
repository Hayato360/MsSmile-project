
services:
  backend:
    build: ./backend
    ports:
      - "8081:8081"
    volumes:
      - ./Mother.db:/app/Mother.db
    restart: unless-stopped

  frontend:
    build: ./frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
    restart: unless-stopped
